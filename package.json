{
  "name": "budgetary-web",
  "version": "0.1.0",
  "private": true,
  "description": "Website for budgetary.tools — pre-flight token-spend estimates for AI coding agents, or an honest \"no read\". Never a confident guess.",
  "type": "module",
  "license": "Apache-2.0",
  "homepage": "https://budgetary.tools",
  "repository": {
    "type": "git",
    "url": "https://github.com/thriftell/budgetary-web.git"
  },
  "scripts": {
    "check:copy": "node scripts/check-forbidden-phrases.mjs",
    "check:install": "node scripts/check-hero-placeholder.mjs",
    "dev": "astro dev",
    "prebuild": "npm run check:copy && npm run check:install",
    "build": "astro build",
    "postbuild": "node scripts/check-forbidden-phrases.mjs dist && node scripts/check-hero-placeholder.mjs dist",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.3",
    "astro": "^5.13.0"
  }
}
